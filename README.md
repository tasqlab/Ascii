# Ascii
text and image to ASCII

## Usage
Enter `text` for text to ASCII  
Then, put in your font. Type `fonts` for a list of available fonts  

Enter `image` for image to ASCII  
Then, enter the image filename (eg. `icon.jpg`)

### Credits
<a href="https://pypi.org/project/pyfiglet/" title="">Pyfiglet</a>  
<a href="https://pypi.org/project/ascii-magic/" title="">ascii-magic</a> 



> I did not make the conversion tools, just put it into a python script
